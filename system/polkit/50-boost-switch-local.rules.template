polkit.addRule(function(action, subject) {
    if (action.id === "io.github.ge777777.boostswitch.set" &&
        subject.local &&
        subject.active &&
        subject.isInGroup("boost-switch")) {
        return polkit.Result.YES;
    }
});
