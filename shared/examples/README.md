# shared/examples

本目录保存当前接口与诊断输出的示例载荷。

当前文件：

- `status-enabled.json`：boost 已开启时的状态示例
- `status-disabled.json`：boost 已关闭时的状态示例
- `diagnostics-group-missing.json`：授权组缺失时的诊断示例

这些文件既用于文档说明，也用于接口回归时的人工核对。
