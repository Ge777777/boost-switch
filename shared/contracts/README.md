# shared/contracts

本目录保存当前仓库已经落地的共享接口契约。

当前文件：

- `io.github.ge777777.BoostSwitch1.xml`：system-bus 上的 D-Bus 接口定义，供后端服务、CLI 和 GNOME Shell 扩展对齐名称、对象路径、方法与信号语义

相关代码同步点：

- `src/boost_switch/contracts.py`
- `gnome-extension/src/lib/dbus.js`
- `tests/test_contracts.py`
